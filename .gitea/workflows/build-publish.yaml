name: Build image
on:
  [push]
jobs:
  build-image:
    runs-on: ubuntu-latest
    container:
      image: catthehacker/ubuntu:act-latest
    env:
      DOCKER_USERNAME: gitea
      RUNNER_TOOL_CACHE: /toolcache # Runner Tool Cache
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 21
          cache: 'npm'
          token: ${{ secrets.NPM_TOKEN }}
      
      - name: Get npm cache directory
        id: npm-cache-dir
        shell: bash
        run: echo "dir=$(npm config get cache)" >> ${GITHUB_OUTPUT}
      
      - uses: actions/cache@v3
        id: npm-cache # use this to check for `cache-hit` ==> if: steps.npm-cache.outputs.cache-hit != 'true'
        with:
          path: ${{ steps.npm-cache-dir.outputs.dir }}
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-                 

      - if: steps.npm-cache.outputs.cache-hit != 'true'
        name: Install dependencies if no cache
        run: npm ci

      - if: steps.npm-cache.outputs.cache-hit == 'true'
        name: Install dependencies if cache
        run: npm i

      - name: Build
        run: |
          npm run build
          npm version patch
          npm set "////git.mb-server.com/api/packages/schoolware-nextgen/npm/:_authToken=${{ secrets.NPM_TOKEN }}"
          npm publish --access public
        env:
          credentials: ${{ secrets.NPM_TOKEN }}